spring:
  application:
    name: service-mregistry

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
  server:
    wait-time-in-ms-when-sync-empty: 0

management:
  tracing:
    sampling:
      probability: 1.0